// @gtaxi/design-system/web — web-safe component re-exports
// Empty by default — web apps import tokens directly.
// Add web-specific component exports here as needed.
export { SURFACE, SHADOW_PROFILE, ANIMATION } from '../../tokens';
